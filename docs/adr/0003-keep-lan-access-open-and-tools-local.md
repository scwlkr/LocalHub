# Keep LAN access open and computer tools local

The Local Gateway accepts connections from the Host's local network without a
Member account or PIN in v1, but this grants inference access only. Computer
and file tools run through a Tool Runner on the caller's own computer with that
person's operating-system permissions; Browser Chat cannot invoke those tools,
and Host files are reachable only when the Host runs tools on the Host Computer.
