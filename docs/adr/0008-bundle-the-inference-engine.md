# Bundle the inference engine

LocalHub v1 is distributed through a zero-cost one-download or one-command
installation that includes a tested llama.cpp version. The Host does not need
LM Studio, Docker, or a source build; after installation, the first `lh` runs
First Run Setup and opens the dashboard.
