# Keep Member history in the browser

Browser Chat history stays with the Member's browser rather than becoming a
shared Host-side history with no identity boundary. Photos and documents may be
uploaded explicitly, but the Host keeps each Attachment only long enough to
process it and then deletes the temporary copy.
