# Never silently change Run Profiles

LocalHub may estimate resource use, warn about risk, and recommend adjustments,
but starting or testing a model uses the exact Run Profile selected by the
Host. A failed Profile Test reports the cause and proposed fixes; LocalHub
applies a change only after the Host explicitly accepts it.
